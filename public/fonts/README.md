# Fonts

`layout.tsx` expects a variable font file at:

```
public/fonts/GeneralSans-Variable.woff2
```

Download it from <https://www.fontshare.com/fonts/general-sans> (free with attribution)
and drop the `.woff2` here before building.

Fraunces loads from Google Fonts at build time and needs no local file.
