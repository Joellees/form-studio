# Wildcard allow so any crawler (Anthropic's web_fetch / ClaudeBot /
# anthropic-ai / Claude-Web — UA name is opaque to us) can fetch the
# preview-token URLs we share into Claude chat. Real access control
# lives at the middleware level (beta gate + Clerk + the preview
# token); robots.txt is advisory and shouldn't be the gate.
User-agent: *
Allow: /

Sitemap: https://formstudio.com/sitemap.xml
